<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unipiloto.lab4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab4">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".eje1"></activity>
        <activity android:name=".eje2"></activity>

        <activity android:name=".eje3"></activity>

        <activity android:name=".eje4"></activity>

        <activity android:name=".eje5"></activity>

        <activity android:name=".eje6"></activity>

        <activity android:name=".eje7"></activity>

        <activity android:name=".eje9"></activity>

        <activity android:name=".eje8"></activity>
        <activity android:name=".eje10"></activity>


    </application>

</manifest>